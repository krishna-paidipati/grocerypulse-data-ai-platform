{
  "$defs": {
    "SupplierType": {
      "description": "Business-defined supplier types.",
      "enum": [
        "LOCAL",
        "REGIONAL",
        "NATIONAL",
        "IMPORTER"
      ],
      "title": "SupplierType",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Represent one GroceryPulse product supplier.\n\nGrain:\n    One model instance represents one product supplier.\n\nPrimary Key:\n    ``supplier_id``\n\nNotes:\n    Supplier reliability scores are represented as a decimal between 0.0\n    and 1.0, where 1.0 indicates the highest reliability.\n    Future planned suppliers may require extending the lifecycle model rather than weakening\n    the current reliability score rule.",
  "properties": {
    "supplier_id": {
      "pattern": "^SUP_\\d{4}$",
      "title": "Supplier Id",
      "type": "string"
    },
    "supplier_name": {
      "maxLength": 120,
      "minLength": 2,
      "title": "Supplier Name",
      "type": "string"
    },
    "supplier_type": {
      "$ref": "#/$defs/SupplierType"
    },
    "country": {
      "maxLength": 80,
      "minLength": 2,
      "title": "Country",
      "type": "string"
    },
    "lead_time_days": {
      "maximum": 365,
      "minimum": 0,
      "title": "Lead Time Days",
      "type": "integer"
    },
    "min_order_qty": {
      "anyOf": [
        {
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Min Order Qty"
    },
    "reliability_score": {
      "anyOf": [
        {
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        {
          "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Reliability Score"
    },
    "is_active": {
      "default": true,
      "title": "Is Active",
      "type": "boolean"
    },
    "created_at": {
      "format": "date-time",
      "title": "Created At",
      "type": "string"
    }
  },
  "required": [
    "supplier_id",
    "supplier_name",
    "supplier_type",
    "country",
    "lead_time_days",
    "created_at"
  ],
  "title": "Supplier",
  "type": "object"
}
